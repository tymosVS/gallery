.container
  %h2= link_to image.title, image_path(image)
  .image
    = link_to image_tag(image.image.medium_thumb.url, 
      alt: 'Image', class: 'img-fluid img-thumbnail '), image_path(image) if image.image?

