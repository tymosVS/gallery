%h2= t('comments.title')
.all-commments
  - @image.comments.each do |comment|
    .comment
      .comment-head
        %span
          = comment.title
        %span{class: 'time'}
          = comment.created_at.strftime('%m-%d  / %I:%M').to_s
      .comment-body
        %span
          = comment.body


