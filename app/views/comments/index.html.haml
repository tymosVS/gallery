%h1= t('comments.title')
.all-comments
  - @comments.each do |comment|
    %div
      %p
        %strong Title:
        = comment.title
      %p
        %strong Comment:
        = comment.body
      %p
        %strong Date:
        = comment.created_at
      -# %p
      -# = link_to 'Delete', [comment.image, comment], |
      -#   method: :delete,                                       |
      -#   data: { confirm: 'Are you sure?' }                     |